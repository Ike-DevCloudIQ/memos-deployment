# Landing-zone namespace for a secrets tool (External Secrets Operator,
# Sealed Secrets, etc.). Placeholder so the `secrets` Argo app is real
# and Healthy. Add the operator's manifests/chart here to grow it.
apiVersion: v1
kind: Namespace
metadata:
  name: external-secrets
  labels:
    app.kubernetes.io/managed-by: argocd
    purpose: secrets-management
